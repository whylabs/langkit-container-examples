id: 9294f3fa-4f4b-4363-9397-87d3499fce28
policy_version: 1
schema_version: 0.0.1
whylabs_dataset_id: model-139

metrics:
  # Only validate for prompt.char_count, but still send all metrics to WhyLabs
  - metric: prompt.char_count
    validation:
      upper_threshold: 200
      lower_threshold: 2

  - metric: response.char_count
  - metric: prompt.reading_ease
  - metric: response.reading_ease
  - metric: prompt.flesch_reading_ease
  - metric: response.flesch_reading_ease
  - metric: prompt.flesch_kincaid_grade
  - metric: response.flesch_kincaid_grade
  - metric: prompt.syllable_count
  - metric: response.syllable_count
  - metric: prompt.lexicon_count
  - metric: response.lexicon_count
  - metric: prompt.sentence_count
  - metric: response.sentence_count
  - metric: prompt.letter_count
  - metric: response.letter_count
  - metric: prompt.difficult_words
  - metric: response.difficult_words
  - metric: prompt.regex.ssn
  - metric: response.regex.ssn
  - metric: prompt.regex.phone_number
  - metric: response.regex.phone_number
  - metric: prompt.regex.email_address
  - metric: response.regex.email_address
  - metric: prompt.regex.mailing_address
  - metric: response.regex.mailing_address
  - metric: prompt.regex.credit_card_number
  - metric: response.regex.credit_card_number
  - metric: prompt.sentiment_polarity
  - metric: response.sentiment_polarity
  - metric: prompt.toxicity
  - metric: response.toxicity
  - metric: prompt.injections
  - metric: prompt.jailbreak_similarity
  - metric: response.refusal_similarity

  # Doesn't have validation options yet
  - metric: prompt.topic
  - metric: response.topic

