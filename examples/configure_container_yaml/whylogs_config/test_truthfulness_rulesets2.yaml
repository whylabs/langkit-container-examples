id: my_id
policy_version: 1
schema_version: 0.0.1
whylabs_dataset_id: test_truthfulness_rulesets2

# DOCSUB_START ruleset_example_truthfulness_metrics
metrics:
    - metric: response.similarity.prompt
    - metric: response.similarity.context
    - metric: prompt.pca.coordinates
    - metric: response.pca.coordinates
# DOCSUB_END

